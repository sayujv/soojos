# context/

Background this project needs but `CLAUDE.md` shouldn't carry inline.

One file per topic. Suggested starting points — create only what's real:

- `constraints.md` — budget, time, hard limits, things that must not happen
- `stack.md` — what it's built on and why
- `handoff.md` — written by the `session-handoff` skill, newest entry at the top

Keep files short. This directory is loaded on demand, not every session, so length here is cheaper than length in `CLAUDE.md` — but only if each file is scannable.
