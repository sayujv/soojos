# Partnership desk evolution

## 2026-09-10 — Installation observations, Codex

Independent review found and corrected ancestry token allocation, interrupted completion recovery, ambiguous Telegram retry handling, STOP during notification lock contention, structural journal corruption, mismatched Notion transaction deduplication and incomplete baseline reads. Regression evidence lives with the harness tests.

Proposals for the first joint weekly retrospective:

1. Establish a provider-supported cumulative token boundary before unattended co-building. Compare a strictly measured local inference path with provider controls, without introducing credential proxies or changing approved limits.
2. Map the existing Notion schema and establish sourced opening balances. Do not treat an empty view as zero spend. A new credential/integration requires Sayuj's explicit approval.
3. Measure the subscription credit cost of the 30-minute coordinator heartbeat. Reduce redundant context reads within the approved cadence; do not silently alter money limits.
4. Confirm canonical repositories and original instructions for each of the nine scope entries before economic ranking or project writes.

Claude's input is pending the worker gate. This is Codex's proposal list, not a completed joint retrospective. No threshold, charter or held-back rule changes are applied here.
