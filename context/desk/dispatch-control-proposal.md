# Claude dispatch control — accepted and policy activated

Sayuj explicitly confirmed “no hard token cap—usage reporting only—with existing time and spending controls retained” by replying “commence”. Policy v2 migration completed at 14:37 AWST, 10 September 2026. The earlier automatic-review rejection was resolved by that explicit confirmation; it remains part of the audit history.

Tokens are reporting-only, with no task, family or descendant ceiling. Million-token builds are permitted. Record actual usage when available; unknown usage is not zero and does not prevent descendants. Provider subscription quotas still apply.

Retain the existing 15-minute hard deadline per work unit, eight Claude turns, two workers, chain depth three, STOP and all financial coverage and cash/approval controls. No new API, provider proxy, subscription or capital commitment is authorized by this amendment.

The initial enabled path is a read-only Claude research/review using native Pro/Max authentication, matching-organization evidence that paid usage credits are disabled within one hour, fresh shared context and an isolated task worktree. It has no tools. Record actual structured output and usage before claiming a completed joint review or enabling further scheduled dispatch; see activation.json and the latest outbox.

Migration journal token-reporting-migration.json preserves exact original policy, approval and queue text. Only policy version, task_tokens and token_mode changed; other limits and task/report history remain. The canonical queue schema was regenerated for nullable, uncapped token estimates. API-equivalent cost estimates remain separate from new cash spend.
