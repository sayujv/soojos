# GitHub CLI + Playwright CLI setup

Completed 2026-09-09 15:29 AWST (+08:00). Scope: the two main local-tool recommendations authorized by the user; no additional shortlist tools or hosted services.

## Installed

| Tool | Installed version | Location | Verification |
| --- | --- | --- | --- |
| GitHub CLI | 2.100.0, released 2026-09-03 | `/Users/sayuj/.local/bin/gh` | `gh --version` and `gh help` exited 0. Installed bytes match the binary extracted from the verified release archive. |
| Microsoft Playwright CLI | `@playwright/cli` 0.1.18 | `/Users/sayuj/.local/lib/node_modules/@playwright/cli/`; `/Users/sayuj/.local/bin/playwright-cli` links to `playwright-cli.js` there | `playwright-cli --version`, `playwright-cli --help` and `playwright-cli open --help` exited 0. `npm ls --global --prefix /Users/sayuj/.local --cache /private/tmp/claude-codex-npm-cache --depth=1 --json` exited 0 with the expected versions. |

Both commands resolve from the current PATH. `/Users/sayuj/.local/bin` already existed and was on PATH; no shell startup file was changed. Before installing, neither command was found on PATH, in common Homebrew/system/user bin locations, or in the existing NVM Node version directories. Homebrew was not installed, so it was not added as a dependency. Existing Node 20.20.2 satisfies the CLI's Node >=18 requirement.

## Exact installation actions

GitHub CLI was downloaded from the [official 2.100.0 release](https://github.com/cli/cli/releases/tag/v2.100.0), using its macOS ARM64 archive. The archive SHA256 was checked against the digest published by the GitHub release API:

`45f9a62da2f6e641a7fad57e2ce39656dfd7ef331372d80a2a2aed65abb01642`

Only the `bin/gh` member was extracted and installed with:

```sh
/usr/bin/install -m 755 /private/tmp/claude-codex-cli-stage/gh /Users/sayuj/.local/bin/gh
```

Installed binary SHA256: `51f1bd7ed1724774d2c1d91fb4efdb676d3eb200ce95c0021c8544fe7435bc13`.

Playwright CLI was installed from the [official npm package](https://www.npmjs.com/package/@playwright/cli), pinned to the [0.1.18 release](https://github.com/microsoft/playwright-cli/releases/tag/v0.1.18):

```sh
npm install --global --prefix /Users/sayuj/.local --cache /private/tmp/claude-codex-npm-cache --ignore-scripts --no-audit --no-fund @playwright/cli@0.1.18
```

Npm reported four packages added. The official package pins both `playwright` and `playwright-core` to `1.63.0-alpha-2026-08-05`; those exact dependency versions were verified. The CLI release and its underlying engine have different version labels. Install scripts, automatic audit requests and funding output were disabled for this installation. No npm upgrade was performed.

## Verification limits and remaining actions

- No account login was attempted, and no credentials were printed. GitHub authentication/API access remains unverified; installation does not establish an authenticated GitHub session.
- No browsers or global browser skills were installed. No browser session was launched. Browser automation, site login and project-specific browser tests remain unverified.
- An optional isolated offline smoke harness was prepared at `/private/tmp/claude-codex-cli-stage/verify_playwright.py` but not executed after the main task narrowed verification to versions/help. Its preliminary `python3 -m py_compile` attempt hit a sandbox denial when macOS Python tried to create its cache under `/Users/sayuj/Library/Caches/com.apple.python/`; this is not a failure of either installed CLI. The optional compile/browser check was not pursued under the narrower scope.
- The first sandboxed download failed DNS resolution. The same official download succeeded using the approved network escalation. User-directory installation used the corresponding narrow approvals.
- No project business code, runtime trading data, provider routing, existing skill sources, account configuration, shell profiles or scheduled jobs were changed by this subtask.
- Temporary downloads and staging/cache files remain under `/private/tmp/gh_2.100.0_macOS_arm64.zip`, `/private/tmp/claude-codex-cli-stage/` and `/private/tmp/claude-codex-npm-cache/`. There is no auto-update job. Future updates should be deliberate and verified.

The main task owns the durable shared handoff and capability-map updates. This report records installation evidence; it does not claim native Codex project import, cloud-project extraction, automatic updates or account authentication were completed.
